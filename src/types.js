import PropTypes from 'prop-types'

export const CaseEnum = PropTypes.oneOf(['camel', 'kebab'])

export const DropdownEnum = PropTypes.oneOf([
  'display',
  'flexDirection',
  'justifyContent',
])
